apply plugin: 'com.android.library'

apply from: '../../base.gradle'

dependencies {

    api('com.github.hslooooooool:abcl-c:1.6') {
        // 排除表单组件
        exclude group: 'com.github.hslooooooool.abcl-c', module: 'core-form'
    }
}